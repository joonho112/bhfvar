# Estimate Sampling Variance for Each Domain

Internal function to estimate the sampling variance of the
domain-specific proportion.

## Usage

``` r
estimate_sampling_variance(domain_summary)
```

## Arguments

- domain_summary:

  Domain summary from compute_domain_summary().

## Value

Numeric vector of sampling variance estimates.
